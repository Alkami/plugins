name: webview_flutter_android
description: A Flutter plugin that provides a WebView widget on Android.
repository: https://github.com/Alkami/plugins/tree/main/packages/webview_flutter
publish_to: https://dart.cloudsmith.io/alkami/flutterpackages/
version: 3.3.0 # original version was 2.8.3 when this was forked

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=2.5.0"

flutter:
  plugin:
    implements: webview_flutter
    platforms:
      android:
        package: io.flutter.plugins.webviewflutter
        pluginClass: WebViewFlutterPlugin

dependencies:
  flutter:
    sdk: flutter
  # FOR TESTING ONLY. DO NOT MERGE.
  webview_flutter_platform_interface: 
    # path: ../webview_flutter_platform_interface
    hosted: https://dart.cloudsmith.io/alkami/flutterpackages/
    version: ^3.0.0

dev_dependencies:
  build_runner: ^2.1.4
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  mockito: ^5.0.16
  mplat_pr_tools:
    git:
      url: https://github.com/Alkami/mplat_pull_request_tools.git
      ref: chore/micaiah/DEV-139004-publish-webview-to-cloudsmith
  pedantic: ^1.10.0
  pigeon: 1.0.9
